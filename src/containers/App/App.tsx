import React from 'react';
import Weather from '../Weather/Weather'

const App: React.FC = () => {
    return (
        <div className="app">
            <Weather />
        </div>
    );
};

export default App;
